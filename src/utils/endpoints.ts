export const SERVER_PORT: number = 5000;
export const CLIENT_PORT: number = 3000;
export const SERVER_URL: string = `http://localhost:${SERVER_PORT}`;
export const CLIENT_URL: string = `http://localhost:${CLIENT_PORT}`;
export const RENDER_PDF_URL: string = `${SERVER_URL}/render`;