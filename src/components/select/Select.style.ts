import styled from 'styled-components';

export const Select = styled.select`
  border-radius: 0;
  border: 1px solid grey;
  padding: 10px;
  background: white;
  cursor: pointer;
  font-size: 14px;
`;
